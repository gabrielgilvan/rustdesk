name: Run flutter-build (caller)

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  call-flutter-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: false
